#--------------------------------------------------------------------
# CRM Backend Environment — example
# Copy this file to `.env` and fill in your values.
#--------------------------------------------------------------------

CI_ENVIRONMENT = development

app.baseURL = 'http://localhost:8080/'
app.forceGlobalSecureRequests = false
app.indexPage = ''

#--------------------------------------------------------------------
# DATABASE (main shared database)
#--------------------------------------------------------------------
database.default.hostname = localhost
database.default.database = crm_main
database.default.username = root
database.default.password =
database.default.DBDriver = MySQLi
database.default.DBPrefix =
database.default.port = 3306

#--------------------------------------------------------------------
# SESSION
#--------------------------------------------------------------------
session.driver = 'CodeIgniter\Session\Handlers\FileHandler'
session.cookieName = 'crm_session'
session.expiration = 7200

#--------------------------------------------------------------------
# CRM
#--------------------------------------------------------------------
crm.clientDbPrefix = client_

#--------------------------------------------------------------------
# INTEGRATIONS — configured IN-APP, not here.
#--------------------------------------------------------------------
# Gmail (admin: Integrations page), per-client email, Google Calendar/Sheets,
# Facebook Lead Ads and the Applicant secondary DB are ALL configured per
# project in the dashboard and stored in the database — there is no shared
# .env fallback (one project never uses another's credentials).
